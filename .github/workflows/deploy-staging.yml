name: Deploy to Staging

on:
  push:
    branches:
      - staging
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Prepare Theme Folder
        run: |
          mkdir deploy_folder
          cp -r functions.php style.css inc JS acf-json deploy_folder/

      - name: Sync to Staging Server
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: '' # Leave blank if using password
          ARGS: "-rlgoDzvc -i"
          SOURCE: "deploy_folder/"
          REMOTE_HOST: "sftp.wp.com"
          REMOTE_USER: ${{ secrets.STAGING_SFTP_USERNAME }}
          REMOTE_PASSWORD: ${{ secrets.STAGING_SFTP_PASSWORD }}
          REMOTE_PORT: 22
          TARGET: "htdocs/wp-content/themes/twentytwentytwo-child/"
          EXCLUDE: "/.git/, /.github/"